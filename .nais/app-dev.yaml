apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: familie-ws-proxy
  namespace: teamfamilie
  labels:
    team: teamfamilie
spec:
  image: {{ image }}
  port: 8080
  webproxy: true
  ingresses:
    - https://familie-ws-proxy.dev-fss-pub.nais.io
  accessPolicy:
    inbound:
      rules:
        - application: familie-oppdrag-backend
          cluster: dev-gcp
  env:
    - name: STS_BASE_URL
      value: https://sts-q1.preprod.local
    - name: CICS_BASE_URL
      value: https://cics-q1.adeo.no
  strategy:
    type: RollingUpdate
  azure:
    application:
      enabled: true
  liveness:
    path: /
    initialDelay: 1
  readiness:
    path: /
    initialDelay: 1
  resources:
    limits:
      memory: 256Mi
    requests:
      cpu: 10m
      memory: 16Mi
  replicas:
    min: 1
    max: 1
  observability:
    logging:
      destinations:
        - id: elastic
        - id: loki
